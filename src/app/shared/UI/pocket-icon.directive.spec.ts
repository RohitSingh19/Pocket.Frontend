import { PocketIconDirective } from './pocket-icon.directive';

describe('PocketIconDirective', () => {
  it('should create an instance', () => {
    const directive = new PocketIconDirective();
    expect(directive).toBeTruthy();
  });
});
